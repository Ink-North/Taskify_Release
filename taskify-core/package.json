{
  "name": "taskify-core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:api": "node scripts/check-public-api.mjs",
    "pretest": "npm run build && npm run check:api",
    "test": "node --test --experimental-strip-types tests/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
